<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :new_club_name %><br />
    <%= f.text_field :club_name %> |
    <%= f.select(:club_name, @clubs.collect {|c| [c.name, c.id]})%>
  </div>
  <div class="field">
    <%= f.label :logo %><br />
    <%= f.text_field :logo %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %> example:  Ballyogan, Carrickmines, Dublin
  </div>
  <!-- <div class="field">
    <%= f.label :longitude %><br />
    <%= f.text_field :longitude %> Leave blank!
  </div>
  <div class="field">
    <%= f.label :latitude %><br />
    <%= f.text_field :latitude %> Leave Blank!
  </div>
  <div class="field">
    <%= f.label :gmaps %><br />
    <%= f.check_box :gmaps %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
